CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    fio VARCHAR(100),
    date_of_birth DATE,
    address VARCHAR(100),
    phone VARCHAR(11),
    experience VARCHAR(100),
    resume VARCHAR(100),
    education VARCHAR(200),
    salary NUMERIC(10, 2),
    position VARCHAR(100),
    gender_id INTEGER REFERENCES genders(id),
    military_registration_id INTEGER REFERENCES military_registrations(id),
    type_of_applicant_id INTEGER REFERENCES type_of_applicants(id)
);

CREATE TABLE users_auth (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    login VARCHAR(50) NOT NULL,
    password VARCHAR(50) NOT NULL
);



postgresql. 
1) напиши запрос на создание таблицы users с полями: ФИО, Дата рождения, Адрес, Телефон, Опыт работы, Резюме, Образование, Зарплата, Должность, Пол(связь с таблицей genders), Воинский учет(связь с таблицей genders123), Вид соискательства(связь с таблицей genders321)
2) напиши запрос на создание таблицы users_auth с полями: логин, пароль.
3) необходимо связать таблицы users, users_auth  один к одному











CREATE TABLE ManagerAuth (
    id SERIAL PRIMARY KEY,
    manager_id INTEGER REFERENCES Managers(id),
    username VARCHAR(50) NOT NULL,
    password VARCHAR(50) NOT NULL
);